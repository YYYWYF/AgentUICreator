# Public frontend endpoint for the one AG-UI Agent Runtime.
# Leave empty to use the development-only Mock AG-UI adapter.
VITE_AGENT_ENDPOINT=

# Do not put API keys or other secrets in VITE_ variables.
