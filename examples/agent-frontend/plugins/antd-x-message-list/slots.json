{
  "assistant-actions": {
    "kind": "list",
    "scope": "thread",
    "description": "Ordered actions rendered under each finalized assistant message.",
    "ownerProps": [
      {
        "name": "messageId",
        "type": "string",
        "description": "Stable AG-UI message identifier.",
        "required": true
      },
      {
        "name": "text",
        "type": "string",
        "description": "Plain-text projection used by message actions.",
        "required": true
      }
    ],
    "fallback": "owner"
  }
}
